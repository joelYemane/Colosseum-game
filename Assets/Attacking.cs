using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Attacking : MonoBehaviour
{
/*
    public GameObject enemyBody; 

    public void Update()
    {
        enemyBody = this.transform.parent.parent.parent.parent.parent.parent.parent.parent.parent.gameObject;
        
    }

    private void OnCollisionEnter(Collision collision)
    {

        if (collision.gameObject.CompareTag("Sword"))
        {
            enemyBody.GetComponent<Animator>().SetBool("Blocked", true);
            NotBlocked();
        }
    }

    public void NotBlocked()
    {
        enemyBody.GetComponent<Animator>().SetBool("Blocked", false);
    }
*/
}
